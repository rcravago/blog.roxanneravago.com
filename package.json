{
  "name": "connect-the-dots",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && cd build && git init . && git add . && git commit -m \"Deploy\"; git push \"git@github.com:rcravago/connectTheDots.git\" master:gh-pages --force && rm -rf .git"
  },
  "author": "Roxanne Ravago",
  "license": "ISC",
  "dependencies": {
    "extend": "^2.0.0",
    "handlebars": "^2.0.0",
    "highlight.js": "^8.2.0",
    "metalsmith": "^1.0.1",
    "metalsmith-collections": "^0.6.0",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-permalinks": "^0.4.0",
    "metalsmith-templates": "^0.6.0",
    "moment": "^2.8.3"
  }
}
